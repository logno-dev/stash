services:
  stash-app:
    build: .
    container_name: stash-app
    ports:
      - "3000:3000"
    volumes:
      - stash-data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped

volumes:
  stash-data:
